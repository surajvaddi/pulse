{
  "name": "@pulseshift/ai",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "lint": "eslint \"src/**/*.ts\"",
    "test": "tsx src/gateway.assert.ts && tsx src/gateway-factory.assert.ts && tsx src/eval-fixtures.assert.ts && tsc --noEmit",
    "test:live": "tsx src/live-smoke.assert.ts"
  },
  "dependencies": {
    "@pulseshift/tools": "*",
    "zod": "^3.24.1",
    "zod-to-json-schema": "^3.25.2"
  },
  "devDependencies": {
    "tsx": "^4.20.6"
  }
}
